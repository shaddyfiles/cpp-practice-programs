#include<iostream>
#include<cmath>
using namespace std;

int main()
{ int a,b,c;
   cout << "type prend" << endl;
   cin >> a;
   cout << "type hpyot" << endl;
   cin >> b;
   c = sqrt((a*a)+(b*b));
   cout << "your hpot :" << c << endl;
    return 0;
}